__pycache__/
*.py[cod]

# 令牌只放各 Skill 根目录的 .env，绝不入库（rankup 的 validate 会断言这一点）
.env
*/.env

# 各 Skill 的工作目录（中间产物、JSONL、探针输出等），不入库
.backlink/

# 当 Skill 源码目录被当作「项目」激活 rankup 时，版本检查会在根目录建 .rankup/skill-state.json
# 这是运行时产物，不入库
.rankup/

# skill-creator 的评测工作目录（agent 输出、评分、benchmark），不入库
*-workspace/

# autopilot 的阶段状态文件,写在仓库根(见 autopilot/SKILL.md「每轮开始时创建 progress.md」)。
# 任务完成后不会自动清理,且正文里带着项目域名、第三方站的评论 ID 与流量读数——
# 属于项目侧,不进这个公开仓库。它同时是滞后指标:实测有过顶层 status="starting"
# 而八个 phase 全是 completed 的,照着它答"接下来做什么"会重做已交付的工作。
# 只忽略仓库根的这一份,不影响任何 skill 目录内同名的参考文件。
/progress.md
/progress-*.md

# 登录面板截图与双证人证据,不入公开仓库
backlink/evidence/

# platforms/ 手册各页的视觉档案(登录面板截图),不入公开仓库
platforms/**/assets/

# agent-fleet 是第一个真正依赖 npm 包(Claude Agent SDK)的工具目录,
# node_modules 里含平台原生 claude 二进制(单个 ~200MB+),绝不入库。
agent-fleet/node_modules/
