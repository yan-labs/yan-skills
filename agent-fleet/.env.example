# agent-fleet 的密钥模板。复制成同目录下的 .env 再填真实值:
#   cp .env.example .env
#
# .env 已被仓库 .gitignore 排除(根 .gitignore 的 `*/.env` 规则),不会被提交。
# 这里列出的每一行只是「models.config.json 里 apiKeyEnv 指向哪个变量名」,
# 具体填哪一行取决于你打算用哪些模型——用不到的模型对应的行可以留空或删掉。

# DeepSeek 官方 API Key。去 https://platform.deepseek.com 的 API Keys 页面生成。
# 对应 models.config.json 里的 deepseek-v4-pro / deepseek-v4.1-flash。
DEEPSEEK_API_KEY=

# Moonshot(Kimi)官方 API Key。去 https://platform.moonshot.cn(或国际站 platform.moonshot.ai)生成。
# 对应 models.config.json 里的 kimi。
MOONSHOT_API_KEY=

# Kollab 官方公开 LLM 网关的账号 key。用 `kollab api-key create --name <名字>` 自助生成
# (账号自助创建,随时可在 Kollab 里吊销/重建,不是 Kollab 内部基础设施密钥),不需要等第三方审批。
# 对应 models.config.json 里的 kollab-gateway,调用费用从这把 key 绑定的 Kollab Space 额度实时扣除。
KOLLAB_LIVE_API_KEY=

# Gemini:Google 官方没有 Anthropic 兼容端点,这里填的是你自己网关认的 Key,
# 不是 Google AI Studio/Vertex 的原生 Key(除非你的网关就是直接透传这个 Key)。
# 先把 models.config.json 里 gemini 条目的 baseURL/model 填成你网关的地址,这一项才有意义。
GEMINI_GATEWAY_API_KEY=

# Typesafe(JEV / System One)官方 API Key。去 https://docs.typesafe.ai/ 对应的账号页面生成。
# 对应 models.config.json 里的 jev。协议是 typesafe-systemone(自有协议,不是 Anthropic/OpenAI
# 兼容),只能用 `agent-fleet judge` 调用,不支持 run/run-many——它不生成文本、不支持多轮工具
# 调用,只接受一段 state + 类型化 questions(noul/choice/score),返回结构化判断,详见 README。
TYPESAFE_API_KEY=

# 自定义请求头(可选)。某些第三方网关/企业代理除了 API Key 还要求一个额外的认证头。
# 这类头的值本身就是凭据,所以规则和 API Key 完全一样:models.config.json 里只写
# 「头名 -> 变量名」的指针,真实值只放这里。例如在 models.config.json 的某个模型条目里加:
#   "headerEnvs": { "X-Gateway-Auth": "MY_GATEWAY_HEADER_TOKEN" }
# 然后在下面填真实值。不需要额外头的模型(DeepSeek/Kimi 官方端点)完全用不到这一节。
# MY_GATEWAY_HEADER_TOKEN=
